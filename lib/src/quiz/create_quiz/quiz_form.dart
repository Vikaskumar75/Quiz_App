import 'package:flutter/material.dart';

class QuizForm extends StatelessWidget {
  const QuizForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Quiz Form'),
      ),
    );
  }
}
