part of 'quiz_form.dart';

class _NoOfQuestions extends StatelessWidget {
  const _NoOfQuestions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const <Widget>[],
    );
  }
}
